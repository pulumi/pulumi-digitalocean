// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * Fetches a list of [DigitalOcean MicroDroplets](https://docs.digitalocean.com/products/microdroplets/) with optional `region` and `name` server-side filters, plus `filter` and `sort` blocks for client-side narrowing.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as digitalocean from "@pulumi/digitalocean";
 *
 * const all = digitalocean.getMicrodroplets({});
 * const nyc3 = digitalocean.getMicrodroplets({
 *     region: "nyc3",
 * });
 * const byName = digitalocean.getMicrodroplets({
 *     name: "my-microdroplet",
 * });
 * const running = digitalocean.getMicrodroplets({
 *     filters: [{
 *         key: "current_state",
 *         values: ["running"],
 *     }],
 * });
 * ```
 */
export function getMicrodroplets(args?: GetMicrodropletsArgs, opts?: pulumi.InvokeOptions): Promise<GetMicrodropletsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("digitalocean:index/getMicrodroplets:getMicrodroplets", {
        "filters": args.filters,
        "name": args.name,
        "region": args.region,
        "sorts": args.sorts,
    }, opts);
}

/**
 * A collection of arguments for invoking getMicrodroplets.
 */
export interface GetMicrodropletsArgs {
    /**
     * Repeatable client-side filter block:
     */
    filters?: inputs.GetMicrodropletsFilter[];
    /**
     * Server-side filter: only include MicroDroplets whose name matches exactly. Conflicts with `region`.
     */
    name?: string;
    /**
     * Server-side filter: only include MicroDroplets in this region. Conflicts with `name`.
     */
    region?: string;
    /**
     * Repeatable client-side sort block:
     */
    sorts?: inputs.GetMicrodropletsSort[];
}

/**
 * A collection of values returned by getMicrodroplets.
 */
export interface GetMicrodropletsResult {
    readonly filters?: outputs.GetMicrodropletsFilter[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of MicroDroplet records. Each record has the same attribute set as the `digitalocean.Microdroplet` data source.
     */
    readonly microDroplets: outputs.GetMicrodropletsMicroDroplet[];
    readonly name?: string;
    readonly region?: string;
    readonly sorts?: outputs.GetMicrodropletsSort[];
}
/**
 * Fetches a list of [DigitalOcean MicroDroplets](https://docs.digitalocean.com/products/microdroplets/) with optional `region` and `name` server-side filters, plus `filter` and `sort` blocks for client-side narrowing.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as digitalocean from "@pulumi/digitalocean";
 *
 * const all = digitalocean.getMicrodroplets({});
 * const nyc3 = digitalocean.getMicrodroplets({
 *     region: "nyc3",
 * });
 * const byName = digitalocean.getMicrodroplets({
 *     name: "my-microdroplet",
 * });
 * const running = digitalocean.getMicrodroplets({
 *     filters: [{
 *         key: "current_state",
 *         values: ["running"],
 *     }],
 * });
 * ```
 */
export function getMicrodropletsOutput(args?: GetMicrodropletsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetMicrodropletsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("digitalocean:index/getMicrodroplets:getMicrodroplets", {
        "filters": args.filters,
        "name": args.name,
        "region": args.region,
        "sorts": args.sorts,
    }, opts);
}

/**
 * A collection of arguments for invoking getMicrodroplets.
 */
export interface GetMicrodropletsOutputArgs {
    /**
     * Repeatable client-side filter block:
     */
    filters?: pulumi.Input<pulumi.Input<inputs.GetMicrodropletsFilterArgs>[] | undefined>;
    /**
     * Server-side filter: only include MicroDroplets whose name matches exactly. Conflicts with `region`.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Server-side filter: only include MicroDroplets in this region. Conflicts with `name`.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Repeatable client-side sort block:
     */
    sorts?: pulumi.Input<pulumi.Input<inputs.GetMicrodropletsSortArgs>[] | undefined>;
}
