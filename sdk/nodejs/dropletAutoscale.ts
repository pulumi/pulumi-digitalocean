// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * Provides a DigitalOcean Droplet Autoscale resource. This can be used to create, modify,
 * read and delete Droplet Autoscale pools.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as digitalocean from "@pulumi/digitalocean";
 * import * as std from "@pulumi/std";
 *
 * const my_ssh_key = new digitalocean.SshKey("my-ssh-key", {
 *     name: "terraform-example",
 *     publicKey: std.file({
 *         input: "/Users/terraform/.ssh/id_rsa.pub",
 *     }).then(invoke => invoke.result),
 * });
 * const my_tag = new digitalocean.Tag("my-tag", {name: "terraform-example"});
 * const my_autoscale_pool = new digitalocean.DropletAutoscale("my-autoscale-pool", {
 *     name: "terraform-example",
 *     config: {
 *         minInstances: 10,
 *         maxInstances: 50,
 *         targetCpuUtilization: 0.5,
 *         targetMemoryUtilization: 0.5,
 *         cooldownMinutes: 5,
 *     },
 *     dropletTemplate: {
 *         size: "c-2",
 *         region: "nyc3",
 *         image: "ubuntu-24-04-x64",
 *         tags: [my_tag.id],
 *         sshKeys: [my_ssh_key.id],
 *         withDropletAgent: true,
 *         ipv6: true,
 *         userData: `
 * #cloud-config
 * runcmd:
 * - apt-get update
 * - apt-get install -y stress-ng
 * `,
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Droplet Autoscale pools can be imported using their `id`, e.g.
 *
 * ```sh
 * $ pulumi import digitalocean:index/dropletAutoscale:DropletAutoscale my-autoscale-pool 38e66834-d741-47ec-88e7-c70cbdcz0445
 * ```
 */
export class DropletAutoscale extends pulumi.CustomResource {
    /**
     * Get an existing DropletAutoscale resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DropletAutoscaleState, opts?: pulumi.CustomResourceOptions): DropletAutoscale {
        return new DropletAutoscale(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'digitalocean:index/dropletAutoscale:DropletAutoscale';

    /**
     * Returns true if the given object is an instance of DropletAutoscale.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DropletAutoscale {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DropletAutoscale.__pulumiType;
    }

    /**
     * The configuration parameters for Droplet Autoscale pool, the supported arguments are 
     * documented below.
     */
    public readonly config!: pulumi.Output<outputs.DropletAutoscaleConfig>;
    /**
     * Created at timestamp for the Droplet Autoscale pool.
     */
    public /*out*/ readonly createdAt!: pulumi.Output<string>;
    /**
     * The current average resource utilization of the Droplet Autoscale pool, this attribute further
     * embeds `memory` and `cpu` attributes to respectively report utilization data.
     */
    public /*out*/ readonly currentUtilizations!: pulumi.Output<outputs.DropletAutoscaleCurrentUtilization[]>;
    /**
     * The droplet template parameters for Droplet Autoscale pool, the supported arguments 
     * are documented below.
     */
    public readonly dropletTemplate!: pulumi.Output<outputs.DropletAutoscaleDropletTemplate>;
    /**
     * The name of the Droplet Autoscale pool.
     */
    public readonly name!: pulumi.Output<string>;
    /**
     * Droplet Autoscale pool health status; this reflects if the pool is currently healthy and ready to accept
     * traffic, or in an error state and needs user intervention.
     */
    public /*out*/ readonly status!: pulumi.Output<string>;
    /**
     * Updated at timestamp for the Droplet Autoscale pool.
     */
    public /*out*/ readonly updatedAt!: pulumi.Output<string>;

    /**
     * Create a DropletAutoscale resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DropletAutoscaleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DropletAutoscaleArgs | DropletAutoscaleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DropletAutoscaleState | undefined;
            resourceInputs["config"] = state ? state.config : undefined;
            resourceInputs["createdAt"] = state ? state.createdAt : undefined;
            resourceInputs["currentUtilizations"] = state ? state.currentUtilizations : undefined;
            resourceInputs["dropletTemplate"] = state ? state.dropletTemplate : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["status"] = state ? state.status : undefined;
            resourceInputs["updatedAt"] = state ? state.updatedAt : undefined;
        } else {
            const args = argsOrState as DropletAutoscaleArgs | undefined;
            if ((!args || args.config === undefined) && !opts.urn) {
                throw new Error("Missing required property 'config'");
            }
            if ((!args || args.dropletTemplate === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dropletTemplate'");
            }
            resourceInputs["config"] = args ? args.config : undefined;
            resourceInputs["dropletTemplate"] = args ? args.dropletTemplate : undefined;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["currentUtilizations"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DropletAutoscale.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DropletAutoscale resources.
 */
export interface DropletAutoscaleState {
    /**
     * The configuration parameters for Droplet Autoscale pool, the supported arguments are 
     * documented below.
     */
    config?: pulumi.Input<inputs.DropletAutoscaleConfig>;
    /**
     * Created at timestamp for the Droplet Autoscale pool.
     */
    createdAt?: pulumi.Input<string>;
    /**
     * The current average resource utilization of the Droplet Autoscale pool, this attribute further
     * embeds `memory` and `cpu` attributes to respectively report utilization data.
     */
    currentUtilizations?: pulumi.Input<pulumi.Input<inputs.DropletAutoscaleCurrentUtilization>[]>;
    /**
     * The droplet template parameters for Droplet Autoscale pool, the supported arguments 
     * are documented below.
     */
    dropletTemplate?: pulumi.Input<inputs.DropletAutoscaleDropletTemplate>;
    /**
     * The name of the Droplet Autoscale pool.
     */
    name?: pulumi.Input<string>;
    /**
     * Droplet Autoscale pool health status; this reflects if the pool is currently healthy and ready to accept
     * traffic, or in an error state and needs user intervention.
     */
    status?: pulumi.Input<string>;
    /**
     * Updated at timestamp for the Droplet Autoscale pool.
     */
    updatedAt?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a DropletAutoscale resource.
 */
export interface DropletAutoscaleArgs {
    /**
     * The configuration parameters for Droplet Autoscale pool, the supported arguments are 
     * documented below.
     */
    config: pulumi.Input<inputs.DropletAutoscaleConfig>;
    /**
     * The droplet template parameters for Droplet Autoscale pool, the supported arguments 
     * are documented below.
     */
    dropletTemplate: pulumi.Input<inputs.DropletAutoscaleDropletTemplate>;
    /**
     * The name of the Droplet Autoscale pool.
     */
    name?: pulumi.Input<string>;
}
