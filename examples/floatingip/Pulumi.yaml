name: floatingip-digitalocean
description: A simple FloatingIp on DigitalOcean.
runtime: yaml

resources:
  ip:
    type: digitalocean:FloatingIp
    properties:
      region: nyc3

outputs:
  ipAddress: ${ip.ipAddress}
